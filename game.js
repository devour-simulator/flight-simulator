import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js';
const mount=document.querySelector('#game'),ui={speed:document.querySelector('#speed'),alt:document.querySelector('#altitude'),heading:document.querySelector('#heading'),start:document.querySelector('#start'),button:document.querySelector('#startButton')};
const scene=new THREE.Scene();scene.background=new THREE.Color(0x82c6e8);scene.fog=new THREE.Fog(0x82c6e8,400,2900);const camera=new THREE.PerspectiveCamera(58,innerWidth/innerHeight,.1,6000);const renderer=new THREE.WebGLRenderer({antialias:true});renderer.setSize(innerWidth,innerHeight);renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.shadowMap.enabled=true;mount.appendChild(renderer.domElement);scene.add(new THREE.HemisphereLight(0xe8f8ff,0x477138,2.2));let sun=new THREE.DirectionalLight(0xfff4d5,2.6);sun.position.set(-400,600,300);sun.castShadow=true;scene.add(sun);
const ground=new THREE.Mesh(new THREE.PlaneGeometry(12000,12000),new THREE.MeshStandardMaterial({color:0x61884b,roughness:1}));ground.rotation.x=-Math.PI/2;ground.receiveShadow=true;scene.add(ground);const grid=new THREE.GridHelper(12000,240,0x8bb36c,0x7fa464);grid.position.y=.2;scene.add(grid);
function mat(c){return new THREE.MeshStandardMaterial({color:c,roughness:.55,metalness:.08})}function box(s,p,m){let z=new THREE.Mesh(new THREE.BoxGeometry(...s),m);z.position.set(...p);z.castShadow=true;return z}
function plane(){let g=new THREE.Group(),white=mat(0xf4f4ed),red=mat(0xb31d29),yellow=mat(0xf1bd2f),dark=mat(0x143b62),glass=mat(0x183f5b);let body=new THREE.Mesh(new THREE.CylinderGeometry(1.28,1.55,12,24),white);body.rotation.z=Math.PI/2;body.castShadow=true;g.add(body);let nose=new THREE.Mesh(new THREE.SphereGeometry(1.3,24,16),white);nose.scale.set(1.18,1,1);nose.position.x=6;g.add(nose);let tail=new THREE.Mesh(new THREE.ConeGeometry(1.25,3.4,24),white);tail.rotation.z=-Math.PI/2;tail.position.x=-7.3;g.add(tail);let stripe=box([8.5,.09,1.05],[0,1.48,0],red);stripe.rotation.z=Math.PI/2;g.add(stripe);let stripe2=box([2.6,.11,1.13],[3.2,1.5,0],yellow);stripe2.rotation.z=Math.PI/2;g.add(stripe2);let wing=new THREE.Mesh(new THREE.BoxGeometry(4.4,.16,13),white);wing.position.set(-.5,-.25,0);wing.castShadow=true;g.add(wing);let tailWing=new THREE.Mesh(new THREE.BoxGeometry(2,.12,5),white);tailWing.position.set(-5.8,.75,0);g.add(tailWing);let fin=new THREE.Mesh(new THREE.BoxGeometry(2.3,3.5,.18),red);fin.position.set(-6.3,2.35,0);fin.rotation.z=-.18;g.add(fin);let logo=box([.08,1.5,1.7],[-6.55,3.35,.13],yellow);g.add(logo);for(let side of[-1,1]){let eng=new THREE.Mesh(new THREE.CylinderGeometry(.72,.88,2.6,20),dark);eng.rotation.z=Math.PI/2;eng.position.set(.2,-1.05,side*4.25);eng.castShadow=true;g.add(eng);let wheel= new THREE.Mesh(new THREE.TorusGeometry(.34,.1,8,16),mat(0x151515));wheel.rotation.y=Math.PI/2;wheel.position.set(-1,-1.8,side*.65);g.add(wheel)}for(let i=0;i<11;i++){let win=box([.1,.35,.18],[3.6-i*.7,.65,1.3],glass);g.add(win);g.add(box([.1,.35,.18],[3.6-i*.7,.65,-1.3],glass))}return g}const jet=plane();jet.position.set(0,22,0);jet.rotation.y=Math.PI/2;scene.add(jet);
const runway=new THREE.Group();let slab=new THREE.Mesh(new THREE.BoxGeometry(80,.35,1400),mat(0x42484b));slab.position.y=.25;runway.add(slab);for(let z=-620;z<=620;z+=55){runway.add(box([3,.12,27],[0,.48,z],mat(0xf7f5df)))}runway.position.set(850,0,-2400);scene.add(runway);let beacon=new THREE.Mesh(new THREE.TorusGeometry(18,1.4,10,36),mat(0xffd339));beacon.rotation.x=Math.PI/2;beacon.position.set(250,40,-700);scene.add(beacon);
let fly=false,spd=280,alt=3200,heading=90,pitch=0,roll=0,keys={},drag=null;function update(){ui.speed.textContent=Math.round(spd);ui.alt.textContent=Math.round(alt);ui.heading.textContent=String((Math.round(heading)+360)%360).padStart(3,'0')+'°'}function begin(){fly=true;ui.start.classList.add('off')}function end(){fly=false;ui.button.textContent='重新起飞';ui.start.classList.remove('off')}ui.button.onclick=begin;
function animate(){requestAnimationFrame(animate);if(fly){let tr=(keys.a||keys.arrowleft?1:0)-(keys.d||keys.arrowright?1:0),li=(keys.w||keys.arrowup?1:0)-(keys.s||keys.arrowdown?1:0);if(drag){tr=drag.x;li=drag.y}roll+=(tr*.55-roll)*.04;pitch+=(li*.3-pitch)*.04;heading+=roll*.65;alt=Math.max(0,alt+pitch*7);spd=Math.max(170,Math.min(430,spd+li*.8));let r=heading*Math.PI/180;jet.position.x+=Math.cos(r)*spd*.02;jet.position.z-=Math.sin(r)*spd*.02;jet.position.y=22+(alt-3200)*.006;jet.rotation.y=-r+Math.PI/2;jet.rotation.z=-roll;jet.rotation.x=pitch;if(alt<8){end()}let desired=new THREE.Vector3(jet.position.x-45*Math.cos(r),jet.position.y+22,jet.position.z+45*Math.sin(r));camera.position.lerp(desired,.04);camera.lookAt(jet.position.x+45*Math.cos(r),jet.position.y-3,jet.position.z-45*Math.sin(r));update()}else{camera.position.lerp(new THREE.Vector3(42,29,55),.025);camera.lookAt(0,21,0)}renderer.render(scene,camera)}animate();update();
addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});addEventListener('keydown',e=>{keys[e.key.toLowerCase()]=true;if(e.key.startsWith('Arrow'))e.preventDefault()});addEventListener('keyup',e=>keys[e.key.toLowerCase()]=false);renderer.domElement.addEventListener('pointerdown',e=>drag={x:(e.clientX/innerWidth-.5)*2,y:(.5-e.clientY/innerHeight)*2});renderer.domElement.addEventListener('pointermove',e=>{if(e.buttons)drag={x:(e.clientX/innerWidth-.5)*2,y:(.5-e.clientY/innerHeight)*2}});addEventListener('pointerup',()=>drag=null);
